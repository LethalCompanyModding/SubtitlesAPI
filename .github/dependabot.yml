version: 2
updates:
  - package-ecosystem: nuget
    directory: /
    schedule:
      interval: weekly
    # Tracks the game's Unity version, which LethalCompany.GameLibs.Steam declares; bump it with the game.
    ignore:
      - dependency-name: UnityEngine.Modules
    groups:
      nuget:
        patterns:
          - "*"
